name: structured
version: 0.1.0
description: "Per-skill/infra long-term memory files with short-term session capture and dreaming consolidation."
hooks:
  - on_session_end
  - on_pre_compress
  - on_memory_write
  - on_turn_start
